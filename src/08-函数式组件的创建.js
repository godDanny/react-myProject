import React,{Component} from 'react';

//使用函数创建组件
// 1.函数名字首字母必须大写
const App=()=>{
    // 2.函数内部必须return一段jsx的代码
    // 3.使用组件的时候，便签必须闭合
    return(
        <div>
            hello world!
        </div>
    )
}

export default App;